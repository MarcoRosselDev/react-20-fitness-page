const SectionHead = ({ icon, title, className }) => {
  return (
    <div className="programs__head">
      <span>{<FaCrown />}</span>
      <h2>Programs</h2>
    </div>
  );
};

export default SectionHead;
